// Where Morse Desktop's releases and its complete source are published (GPLv3 §6: the source is offered from the
// same place as the programs). electron-builder.release.cjs publishes to this repository and the app's updater reads it.
export const releaseOwner = 'bryan910508-del'
export const releaseRepository = 'morse-desktop'
export const releaseSourceURL = `https://github.com/${releaseOwner}/${releaseRepository}`
